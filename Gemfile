source "https://rubygems.org"

# Pulls in the exact Jekyll + plugin versions GitHub Pages builds with,
# including jekyll-seo-tag and jekyll-sitemap — don't list those separately,
# it can trigger a bundler version-resolution conflict against this pin.
gem "github-pages", group: :jekyll_plugins

gem "webrick", "~> 1.8"
